'use client';
import {FormEvent,useEffect,useState} from 'react';import {useRouter,useSearchParams} from 'next/navigation';
export default function LoginClient(){const router=useRouter(),params=useSearchParams();const [mode,setMode]=useState<'login'|'register'>('login'),[username,setUsername]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false);useEffect(()=>{const m=params.get('mode');if(m==='login'||m==='register')setMode(m)},[params]);useEffect(()=>{fetch('/api/auth/me',{cache:'no-store'}).then(r=>r.json()).then(d=>{if(d.user)router.replace('/')}).catch(()=>{})},[router]);async function submit(e:FormEvent){e.preventDefault();setError('');setBusy(true);try{const r=await fetch(mode==='login'?'/api/auth/login':'/api/auth/register',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({username,password})});const d=await r.json().catch(()=>({}));if(!r.ok){setError(d.error||'Impossible de continuer.');return}window.dispatchEvent(new Event('blox:auth'));router.push('/');router.refresh()}catch{setError('Serveur indisponible.')}finally{setBusy(false)}}return <main className="container" style={{maxWidth:520,paddingTop:70}}><section className="game-panel auth-card"><div className="game-kicker">BLOX ARCADE</div><h1>{mode==='login'?'Connexion':'Créer un compte'}</h1><p className="muted">Connecte-toi pour jouer et retrouver ton compte.</p><form onSubmit={submit} className="auth-form"><label>Pseudo<input className="field" value={username} onChange={e=>setUsername(e.target.value)} minLength={3} maxLength={24} autoComplete="username" required/></label><label>Mot de passe<input className="field" type="password" value={password} onChange={e=>setPassword(e.target.value)} minLength={6} maxLength={72} autoComplete={mode==='login'?'current-password':'new-password'} required/></label>{error&&<div className="auth-error">{error}</div>}<button className="playbtn" disabled={busy}>{busy?'Chargement…':mode==='login'?'Se connecter':'Créer mon compte'}</button></form><button className="auth-switch" onClick={()=>{setMode(mode==='login'?'register':'login');setError('')}}>{mode==='login'?"Pas encore de compte ? S'inscrire":'Déjà un compte ? Se connecter'}</button></section></main>}
