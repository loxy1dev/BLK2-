'use client';import {useState} from 'react';import {addCoins,beginGame,endGame,getCoins,recordGameResult,spendCoins} from '@/lib/wallet';const mults=[1.05,1.12,1.22,1.35,1.5,1.68,1.88,2.1];export function Towers(){const [bet,setBet]=useState(100),[started,setStarted]=useState(false),[row,setRow]=useState(0),[revealed,setRevealed]=useState<Record<number,number>>({}),[bombs,setBombs]=useState<number[]>([]),[msg,setMsg]=useState('Choisis ta mise puis lance une partie.');const start=()=>{const b=Math.floor(Number(bet));if(!Number.isFinite(b)||b<10){setMsg('Mise minimum : 10 coins.');return}if(!spendCoins(b)){setMsg('Coins insuffisants.');return}beginGame();setBombs(Array.from({length:8},()=>Math.floor(Math.random()*3)));setRevealed({});setRow(0);setStarted(true);setMsg('Choisis une case à chaque étage.')};const pick=(choice:number)=>{if(!started)return;setRevealed(v=>({...v,[row]:choice}));if(choice===bombs[row]){recordGameResult('Towers','loss',-bet);setStarted(false);endGame();setMsg('💥 Bombe ! Partie perdue.');return}if(row===7){const payout=Math.floor(bet*mults[7]);addCoins(payout);recordGameResult('Towers','win',payout-bet);setStarted(false);endGame();setMsg(`🏆 Tour terminée ! +${payout} coins`);return}setRow(row+1);setMsg(`✓ Sûr — multiplicateur ${mults[row]}x`)};const cash=()=>{if(!started)return;const m=row===0?1:mults[row-1];const payout=Math.floor(bet*m);addCoins(payout);recordGameResult('Towers','win',payout-bet);setStarted(false);setMsg(`🎉 +${payout} coins encaissés`)};return <div className="game-panel"><div className="game-head"><div><h2>🗼 Towers</h2><p className="muted">Grimpe et sécurise ton gain à chaque étage.</p></div><div className="mult">{started?`Étage ${row+1}/8`:'8 étages'}</div></div><div className="betbar"><input className="field" type="number" min="10" value={bet} onChange={e=>setBet(Number(e.target.value))} disabled={started}/><button className="maxbtn" onClick={()=>setBet(getCoins())} disabled={started||getCoins()<10}>MAX</button>{!started?<button className="playbtn" onClick={start} disabled={getCoins()<10}>Lancer</button>:<button className="cashbtn" onClick={cash}>Encaisser</button>}</div><div className="tower">{Array.from({length:8},(_,r)=><div className="tower-row" key={r}>{[0,1,2].map(i=>{const picked=revealed[r]===i,active=started&&r===row;return <button key={i} disabled={!active} className={`tile ${picked?'safe':''}`} onClick={()=>pick(i)}>{picked?'✓':r<row?'✓':'?'}</button>})}</div>)}</div><p className="muted">{msg}</p></div>}
