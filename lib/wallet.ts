'use client';
export const STARTING_COINS=5000;const KEY='blox-arcade-coins';const EVENT='blox:coins:update';export const LIVE_HISTORY_EVENT='blox:live-history:update';const LIVE_HISTORY_KEY='blox-arcade-live-history';
export type LiveHistoryItem={id:string;game:string;type:'win'|'loss';amount:number;createdAt:number};
export function getLiveHistory(){if(typeof window==='undefined')return [];try{const raw=localStorage.getItem(LIVE_HISTORY_KEY),parsed=raw?JSON.parse(raw):[];return Array.isArray(parsed)?parsed.slice(0,5):[]}catch{return []}}
export function recordGameResult(game:string,type:'win'|'loss',amount:number){if(typeof window==='undefined')return;const item={id:crypto.randomUUID(),game,type,amount:Math.floor(amount),createdAt:Date.now()};localStorage.setItem(LIVE_HISTORY_KEY,JSON.stringify([item,...getLiveHistory()].slice(0,5)));window.dispatchEvent(new Event(LIVE_HISTORY_EVENT));void fetch('/api/live-history',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({game,type,amount}),keepalive:true}).catch(()=>{});}
export const GAME_ACTIVE_KEY='blox:game-active';export function isGameActive(){return typeof window!=='undefined'&&localStorage.getItem(GAME_ACTIVE_KEY)==='1'}export function beginGame(){localStorage.setItem(GAME_ACTIVE_KEY,'1');window.dispatchEvent(new Event(EVENT));}export function endGame(){localStorage.removeItem(GAME_ACTIVE_KEY);window.dispatchEvent(new Event(EVENT));}
export function getCoins(){if(typeof window==='undefined')return STARTING_COINS;const raw=localStorage.getItem(KEY);if(raw===null){localStorage.setItem(KEY,String(STARTING_COINS));return STARTING_COINS}const n=Number(raw);return Number.isFinite(n)&&n>=0?Math.floor(n):STARTING_COINS;}
export function setCoins(value:number){const n=Math.max(0,Math.floor(value));localStorage.setItem(KEY,String(n));window.dispatchEvent(new Event(EVENT));return n;}export function addCoins(delta:number){return setCoins(getCoins()+delta)}export function spendCoins(amount:number){const n=Math.floor(amount);if(!Number.isFinite(n)||n<0||getCoins()<n)return false;setCoins(getCoins()-n);return true}export function resetCoins(){return setCoins(STARTING_COINS)}export const COINS_EVENT=EVENT;
